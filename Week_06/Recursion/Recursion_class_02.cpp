#include <iostream>
#include <vector>
using namespace std;
int main()
{

    // int arr[] = {1,2,3,4,,5}

    vector<int> arr = {2, 4, -2, 7, -9};

    // int n = arr.size();

    // int low = 0, high = n - 1;

    // while (low <= high)
    // {
    //     if (arr[low] < 0)
    //     {
    //         low++;
    //     }
    //     else if (arr[high] > 0)
    //     {
    //         high--;
    //     }
    //     else
    //     {
    //         swap(arr[low], arr[high]);
    //     }
    // }

    // for (auto &&i : arr)
    // {
    //     cout << i << " ";
    // }
    //!--------------start recursion class----
    /////

    return 0;
}