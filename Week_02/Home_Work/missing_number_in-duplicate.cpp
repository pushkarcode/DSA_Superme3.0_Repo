#include <iostream>
#include <vector>
using namespace std;
int main()
{
    //!------------visited index method
    vector<int> arr = {1, 3, 5, 3, 4};
    int n = arr.size();

    // for (int i = 0; i < n; i++)
    // {
    //     int index = abs(arr[i]);
    //     if (arr[index - 1] > 0)
    //     {
    //         arr[index - 1] *= -1;
    //     }
    // }

    // for (int i = 0; i < n; i++)
    // {
    //     if (arr[i] > 0)
    //     {
    //         cout << "missing number:" << i+1 << "and duplicate " << arr[i] << endl;
    //     }
    // }

    //!-------------sorting ans swaping method
    
    return 0;
}