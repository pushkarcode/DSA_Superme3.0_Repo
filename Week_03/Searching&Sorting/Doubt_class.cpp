#include <iostream>
#include <vector>
using namespace std;
int main()
{

    // vector<int> arr = {10, 20, 30, 405, 0, 60, 70};
    // int target = 70;
    // auto it = lower_bound(arr.begin(), arr.end(), target);
    // cout << *it << endl;

    //! it uses binary search inside anf give the value which >= target


    //? another is upper case;
    

    return 0;
}