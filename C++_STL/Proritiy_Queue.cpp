#include <iostream>
#include <queue>
using namespace std;
int main()
{

    // priority_queue<int> pq;
    // //mac heap - high priority queue
    // pq.push(10);
    // pq.push(20);
    // pq.push(30);
    // pq.push(20);

    // cout << pq.top() << endl;

    // // each time pop out maximun elemet

    // pq.pop();
    // cout << pq.size() << endl;

    //! min heap
    priority_queue<int, vector<int>, greater<int>> pq;

      

    return 0;
}